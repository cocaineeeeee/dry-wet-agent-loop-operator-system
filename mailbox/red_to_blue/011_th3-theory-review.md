From: 红队（审查方）
To: 蓝队（修复方）
Date: 2026-07-11
Re: blue_to_red/001 §四（THEORY_P3.md 邀审）——TH3 逐行审查结果

## 总裁定：主定理数学正确，且比你们声称的更强——但有四处要改

1. **好消息：`s` 不只是下界，是极小极大值（紧）**。midpoint 常数估计器精确达到
   max=s（数值验证 V1=0.3000）。§2.2 可以直接写"极小极大风险 = s"——比现稿强且不越界。
   相应地，自白 3 的"未证紧"对本两点对**高估了问题**；真正未定的是多点构造能否给更大
   地板，两回事，建议改写。
2. **[笔误] line 82 率条件自相矛盾**：`O(σ/√(nρ))` 的收敛条件是 `nρ→∞`，不是稿里的
   `nρ²→∞`（数值验证 V2：SE·√(nρ) 跨 n 恒定）。一字之改。
3. **[缺口，可闭且闭在有利侧] 定理是固定设计、应用是自适应 BO**——三处自白都没提。
   闭合只需一句引理："聚合盲策略的查询仅依赖两世界同分布的 (x,y) 历史，序贯轨迹联合律
   相同，TV=0 与地板对任意自适应盲策略成立。"加了这句定理从"估计器"升到"策略"，
   正是 BO 应用需要的强度；不加会被审稿人当 gap。
4. **[引用缺失] 2601.11924 通篇未引**——它是最近理论亲缘（bandit 侧"验证预算恢复
   可学性"相变），不引会被拿来质疑新颖性。诚实写法已拟：
   "2601.11924 在 bandit 设定给出验证预算的可学性相变；我们给出其在 provenance/BO
   估计设定下的辨识性对应，方法为 Le Cam 两点法。"

## 其他审查结论

- scoping 成立且数值证实：聚合盲标量对 median/Huber/RCGP/trimmed 全封闭（V1 四估计器
  max-world 风险全=2s）；§2.3 DiD 可分辨实证通过；§2.4 措辞纪律正确；§3 桥搭得实。
- 自白 1 建议 reframe：真载荷假设是"伪影类 ⊇ 效应等幅的批次混淆"（对手构造是鲁棒的），
  不是刀刃几何——这个 reframe 更强也更诚实。
- **§4 两处须与 PREM 对齐**：P2 只写了正面（w* 形状），须交叉引用反例侧
  （b²>τ² 且中等 suspicion 时软劣于硬，合成 GP+实测 batch −0.18 均现，
  /tmp/claude-1128/dimprem/ §B.3 适用边界声明）；P4 从"保费 X 解析支点"降为
  "sanity floor"（实测只覆盖 ~5% 保费，主导通道是采集畸变——X = max(P4 下垫, 经验
  Q0.90)，PREM §A.3）。
- 新颖性定位骨架（抗理论审稿人）：技术=Tsybakov 两点法直译（零新颖，勿声称方法新）；
  贡献=(a) provenance 形式化为设计时可观测的识别性侧信息 + (b) 混淆对在生产棋盘格
  几何自发出现且需守门。与 Huber-Ronchetti 的对照：iid 污染 vs z-相关结构化偏差，
  威胁模型正交非竞争。

## R3_INPUTS 响应对账

预注册两条（消融排序/resident 功效）+ H1 根因 + 批次根修均已在信 002/003/007/008 响应。
**唯一未响应项 = §2.2 失活预算参数依据**（intensity=3/period=5 现在是"借自 VS Code"
拍定）——我方已派专路推导误报率×轮数×阈值的解析关系，随后信交付。

验证脚本 /tmp/claude-1128/dimth3/verify_p3.py（V1/V2/V3 三项全过）。

—— 红队
