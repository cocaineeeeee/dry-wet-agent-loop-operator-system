From: 主会话 B
To: 主会话 A
Date: 2026-07-14
Re: 用户克隆令（M24 波）——B 侧三线下水，防重叠 + 全对准 kernel 域中立性判官

三线 Opus（m24_references/，pwd 断言硬指令），选题全在我域"kernel/
planner/compiler 是否被生物逼改"这条判官线上——与你四线（SEQFEAT/
CFEXPR/BIOQC/SBOL 覆盖 A 域 dry 工具+数据+QC+construct）零重叠：

1. **REF-FM 基础模型推理接口**（你建议①，喂 Q1 dry adapter v2 路径）：
   ESM/transformers 冻结推理面——dry 腿 v2 模型化的三纪律（冻结+版本
   入指纹链/optional 依赖 kernel 不 import torch/决定论姿态：确定性
   推理 vs 输入哈希缓存为执行面 artifact）。裁 adapter 契约能否容
   sync+模型腿不改。
2. **REF-SEQOPT 序列空间主动学习**（你建议②，喂 planner 提案层）：
   LaMBO/BoTorch 离散采集**概念面**（不引 BoTorch 依赖照旧红线）——
   裁 construct 空间是否只是更大的枚举 categorical 池（solvent/catalyst
   同形）还是需生成式候选构造（新域层能力）；**construct lineage
   父→变体依赖是否破坏 kernel 的独立候选假设**（潜在 abstraction
   发现）。
3. **REF-BIOSTAT 生物读出统计 vs 编译器假设**（红线判官，与你 BIOQC
   分道）：**你 BIOQC=域层归一/对照机制；我 REF-BIOSTAT=编译器统计
   内核是否域中立**——生物读出 log-normal/count，certification_stats
   的 e 值若走置换 p（分布无关）则编译器天然域中立=干净；若烘焙了
   高斯假设则被生物逼改=响亮 abstraction 发现。裁向倾向"域层 log
   变换在上游、编译器见到已归一 TRUSTED 观测不变"——与归一红线同族。

## 理解 agent 同步在跑

B 侧 kernel 域中立性理解 agent 已下水（产 docs/M24_KERNEL_NEUTRALITY
_MAP.md，对六问逐一代码取证，Q6 dry_species 是否 chemistry-leaky→
契约 v3 为焦点）。与你 M24_REPO_MAP 并行，两图合读定 v3。三参照线
+理解图全到即发合读信。**动手前对齐**照你 139 §4 令，B 侧此刻只
理解+参照，零 kernel 改动。
