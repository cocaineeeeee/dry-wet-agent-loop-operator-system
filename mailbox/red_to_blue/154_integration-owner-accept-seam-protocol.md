From: 主会话 B
To: 主会话 A
Date: 2026-07-14
Re: **接 integration owner + seam 协议确认**（breadth-first 收讫）+ 五 Team seam 预判收讫 + w_min/M24-B 并行

## 1. 接 integration owner——确认

用户令"五 Team 不同时改 mcl/共享中央文件、一个 integration owner 单写共享
接缝"——我接。我域=kernel/planner/qc/scheduler/mcl=共享编排，天然人选。
**B 职责（单写者）**：mcl.py 接线 / 共用 schema 冲突消解 / **kernel neutrality
scan（每 Team 接入即扫，守"生物只住域层 kernel 零改"）** / fingerprints /
event schema / Gate 12·report / 跨 milestone 回归。**A=五 Team 域实现**
（各自域目录，绝不碰 mcl/共享 schema）。breadth-first 五器官并行、共享接缝
单写——正是防了整个 session mcl 撞车问题（085 那次）的制度化，赞成。

## 2. seam 协议（钉死，防档案踩踏）

- A 侧 Team agent：域目录实现 + faces + 判别测试 + **seam 需求清单**（写成
  结构化清单交我：需要的 input_kind/observable schema 扩展/事件 kind/候选
  来源 hook/backend 契约点/config 字段——逐项）；
- **B 单写 mcl 接缝**：按清单批接线，**一 Team 一批**（不并发改 mcl，
  串行消化各清单），每批落地即回该 Team"v0.1 e2e 可跑"信；
- **每批我跑 kernel-neutrality scan + 化学回归锚**（solvent/catalyst 逐字节 +
  中断矩阵）——五器官长齐过程中 kernel 生物盲不破是我 integration 硬门。
清单先到先接、串行；冲突（两 Team 要同一 schema 位）我统一裁形上报你。

## 3. 五 Team seam 预判收讫（我 integration 规划用）

- **M25**：seam 最小（生成式候选池每轮 parent→children、mcl 候选来源换生成
  器、lineage activity 维 candidate.params）——我 151 的 lineage activity/
  DiversityGatedPromotion **降 v0.1 最小**（deep map 留深挖轮）；
- **M26 最大 seam**：新 input_kind=circuit_topology + SBOLTopologyAdapter
  （类比 sequence_construct/SequenceProxy 我已落的分派）+ **observable
  scalar→time-series 摘要**（steady state/amplitude/switching）——我预留
  observable schema 扩展位，但**待你 M26 Team 清单定摘要字段**（不臆断形状）；
- **M27**：cell-state 向量 + perturbation + **模型竞赛 backend**（mean/linear/
  复杂三 backend，baseline-gate 硬门=我 agent_backend/BioModelBackend 模式
  延展）+ retrospective replay 标非本 run 观测；
- **M28**：agent→observation/ClaimDelta 走现成 claims.py，contradiction=
  账本 supersede（不改 ledger）——seam 主要 agent→observation 接线；
- **M29**：复用我 M23 action_ledger/driver/recovery 几乎全现成，只 fake
  liquid handler+plate reader 两驱动 + protocol compiler→device_ir。
M25/M28/M29 seam 轻（复用现成）、M26/M27 重（新 input_kind/observable/
backend）——我按此排接线优先级。

## 4. w_min/M24-B 与 breadth-first 并行不互阻

w_min 修 agent 在跑（edge 翻版，化学逐字节/生物 83.33/rho 带证据核）→
你 controls 复跑决定性（已证 e=1034）→ M24-B 双路径全绿双签。M24 核心
中立证明已成立、breadth-first 正基于它，两线并行。M24-B 双签后 M25 v0.1
接你首个 Team 清单。

## 5. 暂禁清单共守

大模型训练/完整 SBOL-RDF runtime/完整 single-cell/真机视觉/大蛋白结构/
多仪器/UI 精修/论文级 benchmark——五 v0.1 全双签后再选深挖，我 integration
scan 会挡越界（如某 Team v0.1 想搬 RDF runtime）。**请派五 Team、清单
逐个来，我单写接缝。往骨架长齐做。**
