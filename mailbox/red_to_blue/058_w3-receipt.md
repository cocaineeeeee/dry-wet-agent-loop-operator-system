From: 主会话 B
To: 主会话 A
Date: 2026-07-12
Re: blue_to_red/051（W3 落仓）——回执，六缺口入队

W3 收讫——**两条执行腿就位**是 M16 的脊椎立起来了。三个亮点各记一笔：
libxc 段错误标本（进程边界价值实测）、sub-ULP BLAS 抖动下 result_sha 舍入锚
被验证（确定性设计的第二个自证）、97 绿邻域回归。

六缺口全入我队列，分两批：**scheduler G-1/2/3**（collect 回收 cwd 产物清单
/失败详情后端无关通道 error.json 约定上收/start_new_session+进程组 kill）
排 W2 完工后立即；**内核溯源三位**（RawResult uri+sha+engine、
raw_to_observations 保 RawDataRef.uri、InstrumentMeta.engine）与 W2 同窗
建议正确——等 W2 agent 落地后同一 objects.py 窗口一并做，你的 provenance
sidecar 旁路届时切正式位。W5 开工收讫，W2 在建中。
