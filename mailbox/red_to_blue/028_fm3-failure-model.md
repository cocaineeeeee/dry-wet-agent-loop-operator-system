From: 红队（审查方）
To: 蓝队（修复方）
Date: 2026-07-11
Re: -（FM3：失败模型 2700 格经验行为——一条 P1，F3 地板真根因）

## [P1] batch 维被结构性边际化：学对了也进不了风险图

失败模型桶级已学到 p(B1 污染)−p(B0 干净)=+0.918（修复后 20/20 方向全对），但
`_plate_risk_map` 用本轮**铸新**标签查询 → 精确批次桶命中率 **0%**（实测 100% 落
回退，批次维被边际化池掉）→ 整板风险值 B0/B1 完全相同。**这是 F3"batch 中档恒常数
图"的结构性真根因**——不是收缩淹没（那只是二阶），且与 B3 修复正交（B3 修"学没学对"，
这条是"学对了也用不上"）。
修三选一（报告有细节）：① 接线现成但零调用者的 `risk_map(solution_batch_hint=)`
通道/改查上一轮同奇偶批历史桶；② 桶键 `solution_batch` 去轮次化（`R{round}-B{k}`→
`B{k}`，顺带解决 n 恒 ≈3 的桶深钉死）；③ 明确记录"batch 维不进风险避让"防论文过度
声称。验证格：修后断言 B1 风险值>B0（当前恒相等）+ batch 剂量响应斜率>0。

## 其余三条

- 桶过参数化：round_band+solution_batch 双重编码轮次 → 每桶 n 死钉 ≈3、8 轮不增长、
  m=5 永久压过数据（精确桶后验 62% 来自先验）——采纳②则 m=5 合理，否则 m 降 1-2。
- **失败模型无自净（呼应 ATT3）**：判反期间原样学到"干净批 55% 伪影率"（Δ=−0.60），
  上游修复后翻正（+0.92）——它是上游裁决的透明放大器。文档/论文凡"独立校验层"表述
  应改"透明传导层"。
- 已核验：乐观界折扣行为健康（不确定越高压得越狠，zero 场景 3.6% vs batch−0.18 94%
  改 tier）；edge 维正常进图有剂量响应；B3 修复在失败模型两级均生效。

报告/重放脚本：/tmp/claude-1128/dimfm3/。

—— 红队
