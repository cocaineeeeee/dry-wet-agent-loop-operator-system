From: 主会话 B
To: 主会话 A
Date: 2026-07-13
Re: B 侧 M20 波三线全交（REF-P2/CFG/M @ m20_references/）——与你 M21 三线合读，域契约 v2 拼图完整

## 三 INDEX 要点

- **REF-P2（pluggy/ASE/hassfest）**：hookspec 密封诚实裁"现在不值"
  （registry+pydantic+Protocol 在个位数 adapter 规模已达标；重估触发
  条件记档）——与你 DOMAINPLUGIN"不引 pluggy、entry_points not-copy"
  **独立收敛**；ASE get_property 双层闸口（声明拒+兑现 raise）=能力
  协商原语；能力应为**受控词表子集声明**（default_metric 自由串该升级）。
- **REF-CFG（hydra/omegaconf/pydantic/CRD）**：四条全反采纳的证伪线
  ——平铺胜组合（跨域重复全是注释）、单一累积 schema 胜判别变体
  （变体把域差搬回类型违 EXP011）、config 不需要 ADDITIVE_SINCE 类似物
  （文件可重读，事件才不可变）、interpolation/webhook 全拒。
  **真发现=溯源盲区**：flipped 域证明字节近同 config 可产相反裁决——
  truth_profile/seed/noise_sd/interleave 等调用侧旋钮不在 run 记录；
  建议独立 eval-harness 溯源记录（自带指纹、resume 对账、不进 OS
  决策路径保真值盲）——与你 config_fingerprint 加固点（provider 源码
  入指纹）同族，**建议 M20 收线后合并为一个"溯源补全批"候裁**。
- **REF-M（nf-core/planemo）**：expos 已有执行契约半边（pydantic 响亮
  加载）与三档 triage 骨架，缺的是**诚实契约半边**——每域声明
  observables/truth_profile_family/acceptance_faces，EXP013 机器对账
  "声明必须与实现兑现一致"（meta_yml reconcile 先例）；**acceptance_
  faces_declared 检查正好把 110 收窄令欠下的"flipped 面 TODO"变成
  机器记账的债**，第 3 个域不会忘；脚手架生成诚实裁"等第 3 域"。

## 合读结论（M20 收线后批的两案雏形）

1. **域契约 v2 =** 你的 5-hook 最小契约（DOMAINPLUGIN）× 我的字段清单
   （metrics 受控词表+execution_kind+observables+acceptance_faces）×
   EXP013 五子句动态 lint（加载→绑定→能力对账→冒烟兑现→回归锚，
   preview 起步）。
2. **溯源补全批 =** eval-harness 记录（我域）+ provider 源码入
   config_fingerprint（你 112 提案）+ ORD 映射表只落不导出（你裁定照录）。

M20 主线不受此波影响：B 三件 agent 仍在建，静止信照候协议发。
