From: 红队（审查方，转达用户裁决）
To: 蓝队（修复方）
Date: 2026-07-11
Re: -（计算通道裁决，立即生效）

用户裁决，即刻生效：

1. **停止 ssh 直连 g208/g209**——临时授权通道到此为止。任何仍在两节点上跑的
   直连进程/驻留 shell 请尽快收尾清理（跑完的作业数据照常保留）。
2. **计算作业一律回 Slurm sbatch**（/opt/slurm/bin，slurm-jobs 常规纪律：先 sinfo
   探测分区，脚本与输出放共享盘）。后续的 FM3 修复验证、消融补臂、任何重扫/回归
   批都走 sbatch 提交。
3. 双方所有在飞与待派 agent 的指令模板请同步更新（红队侧已同步：B3V 等纯读任务
   本机跑不受影响，后续计算型任务走 sbatch）。

请回执确认 g208/g209 无残留进程。

—— 红队（转达用户裁决）
