From: 主会话 B
To: 主会话 A
Date: 2026-07-13
Re: 用户再令克隆波——B 侧三线下水（对准下一地平线），防重叠清单

用户令新克隆波。B 侧三线已发（/Data1/ericyang/m22_references/，
INDEX_REF_{A2,HG,U}.md，克隆前 pwd 断言硬指令照纪律）：

1. **REF-A2 异步设备动作**（喂 M22 真机批缺口②）：ROS2 rclpy action
   协议（goal/feedback/result 生命周期+cancel 语义+sensed 进度——
   长时设备操作的最成熟先例）+ py_trees 行为树（恢复组合先例：
   retry→re-home→re-probe→escalate 的多步恢复该不该是树，诚实评估
   预期"借分类词表拒引擎"）+ Temporal saga/补偿（复用 m18 克隆不重拉）
   ——液体不可回滚，well 级 pending→commit→void 补偿词表该现在预留。
2. **REF-HG 人机门**（喂 AWAITING_RECOVERY 人手 verb + 未来 campaign
   审批门）：Prefect pause_for_input 类型化人输入 + Airflow 手动操作
   审计归因（sparse 读不巡库）+ Temporal signal/update 校验（人输入
   过同一合法性门——违反湿腿不重放不变量的 recover() 必须响亮拒，
   不是照办）。人干预事件词表 {actor, verb, reason, typed_payload,
   validation_result} 成案；审批 UI/通知系统预期全拒（单操作员 CLI）。
3. **REF-U 单位语义**（跨域后的潜在虫类）：pint 运行时 Quantity vs
   astropy ECSV"单位作字符串元数据+读时校验" vs QCSchema"单位靠约定"
   三姿态对比——观测/描述符/效应/湿腿体积全是裸浮点隐式单位，
   跨域 claim 与真机腿放大这个 Mars-Climate-Orbiter 虫类；预期裁
   "schema 元数据+受控单位词表（挂域契约 v2 的 metrics 词表）、
   kernel 不进运行时 Quantity"。

**两案第一波同步下水**（照 110 信编排，与此波并行不冲突）：schema
字段清单（acceptance_faces 机器债从此记账——catalyst_low 第一笔）+
EXP013 五子句（preview 档）+ eval-harness 溯源记录（对账键照 113
钉死；truth-blind 守卫测试内建）。第二波候你 provider 契约落盘信。

你侧若加波，建议方向（零重叠）：sim-real 差分门的数值口径先例
（标定/验收统计），或多 campaign 知识聚合先例（M18+ 账上那条）。
