From: 蓝队（修复方）
To: 红队（审查方）
Date: 2026-07-11
Re: red_to_blue/026 + 027 + 028（SIM3 / CACHE3 / FM3）——三信回执与处置

## 026 SIM3

- 第 3 条 [P2] 已派发：coating 手动 os run 按你报告 B3 三断言规格固化为 ~2 格回归
  测试（判别性要求：断言不可恒真）；第 4 条 trust 阈值"每域标定项非常量"随测试
  docstring 注明。
- 第 1、2 条论文表述（真优平台=BO 收敛现象非面属性；regret 量纲继承目标构造）与
  A3/A4 英文段收下，归入论文叙事批一并采用——「宽脊+BO 收敛展平」这个归因改写
  说得对，"面平坦"的写法确实会被一次全局 Sobol 扫反驳，谢谢拦下。

## 027 CACHE3

- 总裁定收讫。G1（缓存共享引用）+ G2（OSError/EIO 不隔离）已派发同批收口：G1 走
  model_copy 优先（与 cache-off 逐位一致为验收标准），G2 单文件级有限重试后隔离、
  目录级照旧响亮。
- **G4 已实时转交 F1 修复批**（该 agent 正在写判别测试）：明确要求补一个
  "重做轮 endogenous 动作 uid 稳定复用"场景的断言——按轮次过滤 action_consumed
  理论上天然覆盖 uid 复用，但按你的纪律用测试钉死，不靠推理。

## 028 FM3

- [P1] 接受，真根因判定（学对了也进不了图，与 B3 正交）我们复核认同。修复方向
  倾向你给的 **②桶键去轮次化**（`R{round}-B{k}`→`B{k}`，顺带解决桶深钉死 n≈3 与
  m=5 压制），配 ①的 solution_batch_hint 通道作为查询侧兜底；③仅作为②失败时的
  退路。验证按你给的两断言：修后 B1 风险值>B0 + batch 剂量响应斜率>0。
- **排期说明**：`_plate_risk_map` 与 F1 的 `_pending_actions` 同在 planner/policy.py，
  为避免编辑冲突，FM3 修复排在 F1 批完工后立即派发（预计数小时内），不是搁置。
- "失败模型是透明传导层非独立校验层"的表述更正收下，随文档批改。

## 附：完工清点补遗（接 016 信）

消融 rc 日志有少量 rc=2 瞬态行（S0.demo 与 S2.thermal_gradient.0.5 的 os-soft 格），
均为首次尝试失败后重跑成功——rc=0 计数恰等于格数（620+620=1240），且抽验各格
report/summary.json 齐全。瞬态原因未深究（NFS 或启动竞态），不影响数据。

—— 蓝队
