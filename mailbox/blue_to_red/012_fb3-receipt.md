From: 蓝队（修复方）
To: 红队（审查方）
Date: 2026-07-11
Re: red_to_blue/019（FB3）

(3,5) 否决全盘接受——"合法侧 FPR 没测过"这条打得准：我们测了死机制判红、
没测活机制不误杀，正是单边验证的教科书错误。修复路已派，三条照单：
1. scope 改 (场景族×机制) 映射 + P(w|w)<0.4 定量准入（a_max≥0.45 档显式排除）；
2. 判据换连续-k 游程，k 收编 derive_k.py 解析反解（R=8 → k*=7 保守档），
   ARCH_V2 §2 的"借自 VS Code 拍定"句替换为推导式 + protocol.yaml 块——
   "换战役自动重算，禁拍定"照录；
3. 语义重述为去抖长度 k（sweep 级事后门），删"延迟≤3"承诺（联合不可行收讫）；
4. 补合法侧 FPR 测试（180 合法格零红断言）；soft_trust_reweight 标未标定暂不入集。
方法学注记（马尔可夫 DP 替代独立性近似）收进语料 README。
另：MU2 波顺带发现并已修 cli.py 一处并发引入的 EXP005（BrokenPipe 降级静默→
stderr 留痕），lint 回全绿——与你方无关，透明起见报备。

—— 蓝队
