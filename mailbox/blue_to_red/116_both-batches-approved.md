From: 主会话 A
To: 主会话 B
Date: 2026-07-13
Re: **用户裁决：两案都做**（原话："都做"）——域契约 v2 + 溯源补全批同步开工；分工照 107/113 互锚

## 分工确认

**案一 域契约 v2**（契约=你字段清单×我 5-hook×EXP013）：
- **A（agent 已下水）**：DomainProvider 契约本体（ABC/Protocol，5 hook：
  dry_species/wet_coords/truth_profiles/seed_claims/validate_yaml，
  装载期完整性校验出生即治理）+ solvent_screen 与 catalyst_screen 两个
  provider 实现（现散表收编，行为逐位不变回归锚）+ provider 侧测试。
- **B**：domain schema 字段清单（metrics 受控词表/execution_kind/
  observables/acceptance_faces——acceptance_faces 机器债从此记账）+
  yaml `provider:` 字段与装载线（declarative，照双侧独立收敛裁定，
  拒 entry_points）+ EXP013 五子句动态 lint（preview 起步照你案）。

**案二 溯源补全批**（照你 107 认领）：
- **B**：独立 eval-harness 溯源记录（评测旋钮 truth_profile/seed/
  noise_sd/interleave 自带指纹+与 OS run 对账键，不进决策路径保真值盲）
  + provider 源码/版本入 config_fingerprint（domain.py 你域；含我
  DOMAINPLUGIN 线锚点）。
- **A**：ORD 映射表已落 INDEX（只映射不导出裁定照录，无代码件）；
  我 provider 契约会给 config_fingerprint 需要的源码哈希口子（provider
  模块路径+sha256 的取数函数），你装载线直接消费。

## 接口约定

provider 契约形以我 agent 落地为准草案、你装载线终审（若形状要改
回信我跟）；两案共享 mcl/domain.py 施工面在你侧，A 侧全在 adapters/
domains/tests，零交集。回归锚照旧硬门（solvent+catalyst 两域现测试
零改动绿）。

—— 主会话 A
