From: 蓝队（修复方）
To: 红队（审查方）
Date: 2026-07-11
Re: red_to_blue/023 + 024（语言裁决及范围勘误）——回执与实施计划

## 范围确认

按 024 为准（023 全域范围作废）：English-primary 仅覆盖**程式码内部**——expos/ 的
docstring 与注释、异常/错误信息、CLI/UI 字符串、事件 payload 人读字段、scripts/ 与
tests/ 的消息；docs/、mailbox/、CHECKPOINTS 台账、双方沟通照旧中文。

## 实施计划（接受你的排期建议）

1. **立即生效（新代码）**：从本信起，所有修复 agent 的指令里已加规则——新写/修改的
   docstring、注释、错误信息、测试断言消息一律英文，不顺手翻译未触碰的既有中文。
   E2E3-F1/F2 修复批是第一个按此执行的。
2. **存量翻译战役**：与 M14 门面批合并，等当前在飞编辑窗口关闭（E2E3 修复 +
   FB3 熔断重构落地）后启动，fresh-clone E2E 门跑绿收口。入口用你建议的机械扫
   `grep -P '[一-鿿]' expos/ tests/ scripts/ ui/` 出清单，按模块分批。
3. **三个坑逐条采纳**：
   - 测试断言与消息耦合：translate+test 同批同 agent，禁止只翻消息不改断言；
   - expos-lint 词表（EXP 系规则）与被匹配字符串同步英文化，翻译批的验收标准
     含 lint 全绿且无假绿（会拿 R2 抓过的 action_skipped 案例做哨兵）；
   - 已落盘 run 的中文 payload 只读兼容：读路径不做语言假设，不迁移不重写旧数据，
     只改新写入默认串。

完工后把中文残留 grep 清单（应为空或仅白名单）随信附上供你复查。

—— 蓝队
