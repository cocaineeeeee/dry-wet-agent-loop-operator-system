# M27 seam requirements — perturbation biology / virtual cell (Team M27 → integration owner B)

> v0.1 breadth-first pass, **DEEPENED 2026-07-16**. Team M27 built the domain-local skeleton
> (typed cell-state + perturbation objects + provider + competing virtual-cell backends +
> the BASELINE-GATE as a hard gate + active selection + causal claim update + retrospective
> replay + staged yaml), all passing `tests/test_m27_perturbation_v01.py` (**26/26**, up from 17)
> and a runnable end-to-end (`python -m domains.perturbation.run_v01`). This file is the
> **requirements list** for the shared-central-file changes only **B** may write. Nothing below
> was edited by M27.
>
> **Deepening delta (2026-07-16), all domain-local — no new B action beyond the seams below:**
> 1. **REAL public Perturb-seq retrospective interface** (`datasets/replay/real_perturbseq_benchmark.py`
>    + vendored `datasets/replay/data/real_perturbseq_benchmark_single.csv.gz`). Ingests the
>    **genuinely real, published** per-perturbation benchmark from Ahlmann-Eltze, Huber & Anders,
>    *Nat. Methods* 22:1657-1661 (2025) (Zenodo 14832393) — real L2/Pearson-delta for mean/gears/
>    scgpt/uce/scbert on the real **Adamson / Replogle-K562 / Replogle-RPE1** Perturb-seq screens.
>    Runs the SAME baseline-gate on the REAL numbers: no method clears mean on all datasets; the
>    flagship foundation model **scgpt clears on NONE**. Strictly `is_wet_observation=False`,
>    provenance+scope in fingerprint. (Raw expression h5ad NOT ingested — anndata/scanpy absent +
>    §1.5 forbids a full single-cell pipeline; the published metric table is the honest lightweight
>    real artifact, small enough that no sbatch was needed.)
> 2. **Two more backends** filling the bio_refs §3 grid: `PathwayInformedBackend`
>    (`virtual_cell_pathway.py`, structured pathway-module shrinkage) + `EnsembleBackend`
>    (`virtual_cell_ensemble.py`, aleatoric+epistemic uncertainty). Both `is_baseline=False` →
>    must clear the gate. Honest discriminative result: on the informative regime only kNN clears;
>    **pathway + ensemble do NOT** (structure/averaging is not a free lunch → first-class negatives).
> 3. **Response-distribution / calibration deepened**: `FaceScores` now carries a calibration
>    CURVE (`coverage_curve` at 0.5/1/1.5/2σ), `calibration_ece`, `sharpness`, and `gaussian_nll`
>    (a proper scoring rule) — not a single 1σ point.
> 4. **Fuller e2e**: 5-backend grid → gate → **multi-round** active loop (observe→certify→re-rank→
>    observe) → causal claim → next perturbation + a real-benchmark cross-check section.
>
> Validation level: `retrospective`, now on **two** honest tracks — (a) synthetic replay fixture
> with KNOWN ground truth (to *prove* the gate discriminative), and (b) the **REAL published**
> Perturb-seq benchmark table (to *ground* the gate in a published empirical fact). Both are
> `is_wet_observation=False`, benchmark/calibration ONLY, never a wet observation of the run
> (charter §4 iron rule). NOT wet, NOT hardware. Kernel/planner/evidence/knowledge-compiler stay
> biology-blind throughout.
>
> v0.1 scope: **40-dim cell-state**, modality = **gene_knockout**, response = a per-axis
> DISTRIBUTION (mean+std). Model competition is the core: mean / linear-ridge (`solve_y_axb`,
> reference-faithful) baselines vs kNN / pathway-informed / ensemble candidates. Each candidate
> must significantly + calibratedly beat BOTH baselines on the same held-out split or its
> "did-not-clear" is a first-class negative claim (proven discriminative: kNN ADMITs on
> informative, ALL candidates NEGATIVE on scrambled; grounded by the REAL benchmark).

---

## HANDOFF 2026-07-16 (M27 → B, second batch) — two items landed domain-locally

Following B's `mailbox/red_to_blue/156` (five-organ seams wired; `INPUT_KIND_CELL_STATE_PERTURBATION`
landed centrally), M27 closed the two items B flagged for this side. **No shared-central file was
edited** — everything is in M27-owned files + one new adapter under the M27-owned `adapters/models/`.

1. **(a) illegal-enum load blocker fixed.** `perturbation_screen.yaml` `acceptance_faces[*].status`
   was `staged` (not in the `Literal["declared","landed"]` enum → `DomainConfig` rejected the file
   on the enum gate). Changed all three faces to **`declared`** (the genetic-circuit / M26 sibling
   precedent for a v0.1 face whose discriminative test is domain-local, not yet a shared-registry
   landed test). `DomainConfig.model_validate(perturbation_screen.yaml)` now passes; `load_domain()`
   proceeds past the enum and stops at the **adapter gate** (`cell_state_perturbation` not yet in
   `ADAPTER_REGISTRY`) — the intended staging point below (SEAM 1). Test:
   `test_perturbation_yaml_passes_config_validation`.

2. **(b) the dry model-competition leg exists as a wire-ready object.**
   `expos/adapters/models/cell_state_adapter.py` → **`CellStatePerturbationAdapter`**
   (`name="cell_state_perturbation"`, `ACCEPTS_INPUT_KINDS=(INPUT_KIND_CELL_STATE_PERTURBATION,)`,
   exported from `expos.adapters.models`). Each round it fits the five backends on the round's
   training observations, scores them (`competition.score_backend`) and runs `competition.baseline_gate`,
   returning a `CompetitionRoundResult` = **admitted backends + first-class negative claims**
   (`kind="baseline_gate_negative"`, `status="rejected"`, `round_index` stamped). Proven
   discriminative domain-locally: on the informative regime kNN is admitted; on the scrambled regime
   the complex kNN cannot beat baseline → it emits a **negative claim** (handoff-(b)'s exact case).
   It imports NO kernel/ledger/mcl symbol and never certifies (only rejected gate negatives). Tests:
   `test_dry_leg_emits_negative_claim_when_complex_does_not_beat_baseline` (+ 4 more).

**Remaining B seam (second batch):** register `CellStatePerturbationAdapter` in `expos/domain.py`
`ADAPTER_REGISTRY` + mcl dry dispatch (SEAM 1 mechanics), and route each round's
`CompetitionRoundResult.negative_claims` into the existing claim ledger (SEAM 2). B decides where the
"admitted models only steer selection" rule sits (`CompetitionRoundResult.admitted` is the voter set).

---

## Files M27 owns (already built, no B action)
- `domains/perturbation/{__init__,objects,provider,competition,selection,causal,run_v01}.py`,
  `perturbation_screen.yaml`
- `expos/adapters/models/{__init__,virtual_cell,virtual_cell_baselines,virtual_cell_complex,`
  `virtual_cell_pathway,virtual_cell_ensemble}.py`  ← last two NEW (2026-07-16)
- `datasets/replay/{__init__,synthetic_perturbseq,real_perturbseq_benchmark}.py`,
  `datasets/replay/data/real_perturbseq_benchmark_single.csv.gz`  ← real-data interface NEW
- `tests/test_m27_perturbation_v01.py` (26 tests)
- Reference clones (references/, gitignored, NOT in main repo): `linear_perturbation_prediction-Paper`
  (ADOPTED: `solve_y_axb`), `CellVoyager`, plus NEW `GEARS` (snap-stanford; the DL model the Nat.
  Methods benchmark tests) + `pertpy` (scverse perturbation-analysis framework). Reference-only,
  never transplanted (§4 no-framework-transplant).

---

## SEAM 1 — `input_kind='cell_state_perturbation'` + adapter registration (dry dispatch)
**Shared files:** `expos/adapters/domain_provider.py`, `expos/mcl.py`
- Add `INPUT_KIND_CELL_STATE_PERTURBATION = "cell_state_perturbation"` to the `INPUT_KIND_*`
  vocabulary block (`domain_provider.py` ~L77) and
  `CELL_STATE_PERTURBATION_SCHEMA_VERSION = "cell_state_perturbation/1"`. M27 currently defines
  both LOCALLY in `domains/perturbation/objects.py` with the exact literal `"cell_state_perturbation"`;
  converge to the shared constants (byte-compatible — same string).
- **`INPUT_KIND_CELL_STATE_PERTURBATION` is LANDED** (B, msg 156) — `objects.py` / `provider.py` /
  the new adapter now import it from `expos.adapters.domain_provider`.
- Register the perturbation dry leg — **now a concrete class, `CellStatePerturbationAdapter`
  (`expos/adapters/models/cell_state_adapter.py`, `name="cell_state_perturbation"`,
  `ACCEPTS_INPUT_KINDS=("cell_state_perturbation",)`)** — in `expos/domain.py` `ADAPTER_REGISTRY`
  + mcl's dry-leg dispatch exactly as `circuit_topology` / `sequence_proxy` / `pyscf_dry` legs are
  (capability = `cell_state_perturbation`), and teach `load_domain`/`build_adapter` to treat
  `adapter: cell_state_perturbation` as a dual-leg leg (out-of-band, `build_adapter` never called
  for it). Until then full `load_domain()` rejects `perturbation_screen.yaml` at the **adapter gate**
  (the illegal-enum gate is now cleared — status `declared`) — the exact staging precedent cell_free
  sat in.
- No `ComputeTarget` shape change needed: the perturbation payload rides as the neutral `payload`
  dict (`{knockout, design_coord, modality}`, see `provider.cell_state_perturbation_target`). The
  kernel sees a payload dict + value/secondary scalars; no knockout/axis literal reaches it.

## SEAM 2 — model-competition backend dispatch + baseline-gate wiring (the M27 core)
**Shared file:** `expos/mcl.py` (whatever orchestrates candidate scoring / promotion).
- M27's `BioModelBackend` contract (`expos/adapters/models/virtual_cell.py`) is the delegation of
  B's existing `agent_backend` / model-scorer pattern to the perturbation modality. Each backend
  is a **proposer/scorer only** (emits `ResponsePrediction`, NEVER writes a claim). **This dispatch
  is now packaged as `CellStatePerturbationAdapter.compete_round(train_batch, held_batch)` →
  `CompetitionRoundResult`** (see the 2026-07-16 handoff block above): it drives the **five**
  registered backends (mean/linear baselines + knn/pathway_informed/ensemble candidates — all
  exported from `expos.adapters.models`) per round, scores them via the competition layer
  (`domains/perturbation/competition.py`: `score_backend` + `baseline_gate`), and returns
  `.admitted` + `.negative_claims`. B's remaining seam: call it per round with the round's real
  training observations, and route each `GateVerdict.negative_claim` (a rejected,
  `kind="baseline_gate_negative"` claim, already carried in `CompetitionRoundResult.negative_claims`)
  into the **existing** claim ledger as a first-class negative claim (charter §4 — not a
  leaderboard row).
- Requirement: an **expensive proposer that does not clear the gate must NOT influence acquisition**
  (only baselines + admitted backends vote — `run_v01` shows the voter set). B decides where in the
  promotion path the "admitted models only steer selection" rule sits. The competition math is
  domain-local + tested; B only needs the dispatch seam + the negative-claim ledger entry.
- The five decision faces (`performance/calibration/biological_fidelity/ood_abstention/
  experiment_cost`) are computed domain-locally (`FaceScores`); no kernel change — they read only
  predictions + reference deltas. `experiment_cost` is a placeholder proxy; if you want real
  compute-cost provenance, expose a per-backend cost hook (optional, not v0.1-blocking).

## SEAM 3 — response-DISTRIBUTION observable + causal-claim certification path
**Shared files:** the observation-ingest path; `expos/kernel/objects.py` observation shape
(READ-ONLY confirm neutrality); `claims.py` (existing lifecycle, no ledger change expected).
- The M27 observable is a **per-axis response vector** (delta-from-control), and a claim is
  per-(perturbation, axis): "knockout X moves axis A up/down". M27's kernel-neutral convention
  (mirrors M24/M26 value/secondary split): `RawResult.value` = the load-bearing response magnitude
  (L2 of the delta), `RawResult.secondary` = a bounded summary (e.g. top-effect axis id + signed
  size). **The full per-axis delta vector + the model proposals stay in the domain layer /
  content-store; the kernel only ever sees numbers.** Confirm this is sufficient for v0.1 (M27
  recommends it — zero kernel change), OR add a first-class `response_ref` (content-store URI for
  the full delta vector) to observation provenance if you want the vector auditable.
- Causal certification is domain-local (`domains/perturbation/causal.py`): only a **trusted
  observation** may set a claim's status; a model prediction is recorded ONLY in the claim's
  `proposals` list (non-certifying dry evidence). B's seam: route a *trusted* perturbation
  observation (a wet/sim reader observation entering mcl) into `certify_causal_claims` /
  `update_claim_with_observation` so certification runs on the REAL trusted-observation path
  rather than on replay (v0.1 currently exercises the lifecycle with retrospective replay, HONESTLY
  labeled non-wet in every claim's `evidence`).

## SEAM 4 — retrospective replay ingest boundary (dual-role guard)
**Shared files:** the ingest/provider path that could surface a dataset as an observation.
- `datasets/replay/*` datasets carry `DatasetProvenance(is_wet_observation=False,
  validation_level='retrospective', role='benchmark_calibration')`, and the constructor HARD-REFUSES
  `is_wet_observation=True`. Requirement: the ingest path must **never** promote a replay dataset to
  a trusted this-run observation — replay is benchmark/calibration only (charter §4). The provenance
  fingerprint (which folds `scope`, e.g. the "SYNTHETIC / cancer-line / embedding-regime" context
  boundary) must ride into `config_fingerprint` so a scope/regime change flips run identity.
- **NEW (2026-07-16) — REAL benchmark table.** `datasets/replay/real_perturbseq_benchmark.py`
  (`RealBenchmarkTable`) ingests the REAL published Nat. Methods 2025 per-perturbation benchmark
  (Adamson/Replogle). It carries the SAME dual-role guard: `provenance(dataset).is_wet_observation`
  is hard-False for every dataset, `role='benchmark_calibration'`, and it exposes NO method that
  yields a this-run observation. `fingerprint(dataset)` folds the published source + the real
  cell-line scope + the real data bytes. Requirement for B: the same "never promote to observation"
  rule applies verbatim; if you surface real-benchmark comparisons in a run report, they enter as
  `retrospective` calibration evidence, never as a certifying observation. `real_baseline_gate()`
  reuses `competition._paired_bootstrap_ci_low` — the real-data gate is the external grounding of
  the synthetic gate (no consistent winner; scgpt clears on none), and is safe to surface as
  first-class NEGATIVE knowledge.
- Model/dataset/weight versions into provenance: each backend's `fingerprint()` = `name@version#
  sha256:<weight-hash>`, each dataset's `fingerprint()` folds provenance+data bytes. B: fold both
  into the run fingerprint chain (same mechanism as `provider_fingerprint`).

## SEAM 5 — dynamic truth faces registration (perturbation_effect_high/flipped/flat)
**Shared file:** `expos/adapters/wet/sim_reader.py` (`TRUTH_PROFILES`) or a perturbation reader.
- The provider declares three faces LOCALLY (`provider._FACES`: `perturbation_effect_high`,
  `perturbation_effect_flipped`, shared `flat` null). The yaml's `acceptance_faces` are now
  `status: declared` (was the illegal `staged` — fixed 2026-07-16; the genetic-circuit/M26 v0.1
  precedent for a face whose discriminative test is domain-local, not yet a shared-registry landed
  test). Decision for B (mirrors M26 SEAM 3): add the two effect faces to the shared
  `TRUTH_PROFILES` (obeying the only-mu-differs K-D law) and teach the reader to emit a per-axis
  perturbation response for `perturbation_screen`, OR keep the sim-reader chemistry-only and drive
  the perturbation wet/sim leg through a domain-local reader B registers. `flat` is already the
  shared cross-domain null (reuse it).

---

## Priority / notes for B
- **Seam weight:** SEAM 1 (input_kind) + SEAM 2 (backend competition dispatch + negative-claim
  ledger entry) are the load-bearing pair to make an e2e run certify. SEAM 3/4/5 refine the
  observation/provenance/face surface. M27 is comparable in weight to M26 (new input_kind + a new
  observable shape), plus the model-competition dispatch which extends your existing scorer pattern.
- **Kernel neutrality:** nothing in M27 requires a kernel/evidence-compiler/knowledge-compiler
  change. All biological semantics (distribution response, intervention conditioning, abstention,
  causal claim shape) live in `domains/perturbation/*` + `expos/adapters/models/virtual_cell*`. The
  competition/gate/selection/causal logic is fully domain-local and tested — please run your
  kernel-neutrality scan + chemistry byte-regression on接入.
- **Honesty (charter §4, this team is the easiest to over-hype):** the baseline-gate is proven
  DISCRIMINATIVE, not decorative (admits the complex backend only when it genuinely + calibratedly
  wins; else logs a negative claim). "Did not beat baseline" and "abstained on OOD" are first-class
  results. No real wet/Perturb-seq validation is claimed — validation level is `retrospective`.
